# $schema: https://json.schemastore.org/github-workflow.json

name: Check - Build

on:
  workflow_dispatch:
  push:
    branches:
      - master
  pull_request:

permissions: {}

jobs:
  build:
    name: Build
    uses: rbrownwsws/apko-workflows/.github/workflows/build.yaml@9d5679acbb8dbf79bdbda176f87c64911bedca1e # master
    with:
      image-name: gghait
      image-tag: latest
    permissions:
      contents: read
